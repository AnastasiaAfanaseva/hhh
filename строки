#включить<библиотеки iostream>
#включить<попечительством.ч>
используя пространство имен СТД;
пустота основной()
{
	гольца ул.[10],ТТ[10],РР[10],*п=новый тип char[10];
	инт и=0;
	то scanf("%С %С %С",&стр&ТТ,РР);
	функции strcat(str,на ТТ);
 ул.<<суда;
 р=strstr(ул.,РР);
при(функция strlen(п))
	{
	если (РР-ул!=0)
	{
 р=strstr(р,РР);
		если (п==нуль)
		{
			перерыв;
		}
 р++;
 с++;
	    //функции cout<<с;
	}
	еще
	
 соиь<<"чистая vhog";
	
}
функции cout<<с;
	система("пауза");
}
